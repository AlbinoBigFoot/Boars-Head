# ROADMAP slice — Groveport KPI Nav

Continues Phase 3 HMI (navigation / templates). Sub-phases for this branch:

## Phase G1 — Nav clock (REQ-KPI-01)
Date/time under search in `00_Docked/Navigation`.

## Phase G2 — Home cards (REQ-KPI-02)
Lightspeed MapLayout-inspired KPI cards on BH LandingPage/Globe (views only).

## Phase G3 — Data foundation (REQ-KPI-03, REQ-KPI-04)
Workbook in `docs/kpi/` + placeholder tag folder under `[default]` (e.g. `KPI/Groveport/...`).

## Phase G4 — KPI dashboard views (REQ-KPI-05)
Shared KPI page shell + EPMS / Utility / Production views for Groveport areas.

## Phase G5 — Nav tree + routes (REQ-KPI-06)
Update Navigation / page-config / Nav script so Area → domain leaves open the dashboards.

**Execution:** research subagents per phase → implement → repair signatures → scan → update change log → commit when asked.
