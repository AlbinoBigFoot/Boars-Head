---
phase: 260730-l0s-fix-shared-faceplate-broken-tab-view-pat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gateways/standard/data/projects/BH/com.inductiveautomation.perspective/views/01_Popups/00_Faceplates/Compressor/Controls/resource.json
  - gateways/standard/data/projects/BH/com.inductiveautomation.perspective/views/01_Popups/00_Faceplates/Compressor/Configuration/resource.json
  - gateways/standard/data/projects/BH/com.inductiveautomation.perspective/views/01_Popups/00_Faceplates/_Assets/Alarms/view.json
  - gateways/standard/data/projects/BH/com.inductiveautomation.perspective/views/01_Popups/00_Faceplates/_Assets/Trend/view.json
  - gateways/standard/data/projects/BH/com.inductiveautomation.perspective/views/01_Popups/00_Faceplates/_Assets/Trend/resource.json
  - gateways/standard/data/projects/BH/com.inductiveautomation.perspective/views/01_Popups/00_Faceplates/_Assets/AlarmConfiguration/view.json
  - gateways/standard/data/projects/BH/com.inductiveautomation.perspective/views/01_Popups/00_Faceplates/Faceplate/view.json
  - gateways/standard/data/projects/BH/com.inductiveautomation.perspective/stylesheet/stylesheet.css
  - gateways/standard/data/config/resources/core/ignition/tag-type-definition/default/Devices/udts.json
  - gateways/standard/data/config/resources/core/ignition/tag-definition/default/Compressors/udts.json
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
  - D-07
user_setup: []

must_haves:
  truths:
    - Controls and Configuration tabs load their EmbeddedViews (no View Not Found) after CAS repair + scan
    - Alarms tab renders Alarm Status Table (`ia.display.alarmstatustable`) filtered to device tagPath
    - Trend tab shows in-popup Kyvis ApexChart with pens auto-discovered via recursive browse + Enabled/HistoryEnabled/numeric (not AdhocTrend)
    - Faceplate tab labels are readable dark text on light tab chrome (no inverse button font on tabs)
    - Alarm Configuration remains usable (improved short-name list or honest stub that does not break)
    - Compressor demo FLA/SVP/DisP/Amps Value tags have history enabled so Trend pens appear in sim
  artifacts:
    - gateways/standard/data/projects/BH/com.inductiveautomation.perspective/views/01_Popups/00_Faceplates/_Assets/Trend/view.json
    - gateways/standard/data/projects/BH/com.inductiveautomation.perspective/views/01_Popups/00_Faceplates/_Assets/Alarms/view.json
    - gateways/standard/data/projects/BH/com.inductiveautomation.perspective/views/01_Popups/00_Faceplates/Faceplate/view.json
    - gateways/standard/data/projects/BH/com.inductiveautomation.perspective/stylesheet/stylesheet.css
  key_links:
    - Faceplate EmbeddedView path expression → Controls/Configuration/Trend/Alarms/AlarmConfiguration (paths already correct; CAS must resolve)
    - Faceplate EmbeddedView params.tagPath + params.hiddenTags → _Assets/Trend browse filter
    - _Assets/Trend custom.sensors browse → custom.chartDataAnalog queryTagHistory → ApexChart series
    - repair-resource-signatures.py → projects/.resources CAS digests → POST scan/projects (+ scan/config for tag edits)
---

<objective>
Fix BH shared Faceplate tabs: repair CAS so Controls/Configuration load (D-01), correct Alarms component type (D-02), replace `_Assets/Trend` AdhocTrend stub with Scout-style in-popup ApexChart auto-pens (D-03), fix tab text contrast (D-04), modest Alarm Config honesty/UX (D-05), ensure Compressor analog history (D-06), then signatures + scan + load verify (D-07).

Purpose: Operators can use Controls, Configuration, Trend, Alarms, and Alarm Configuration on the shared Faceplate without View Not Found, missing components, invisible labels, or wrong Trend product.
Output: Repaired Controls/Config CAS; fixed Alarms + Trend + Faceplate chrome; history-ready compressor analogs; clean signature check.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/quick/260730-l0s-fix-shared-faceplate-broken-tab-view-pat/CONTEXT.md
@.planning/quick/_faceplate-diag.md
@.planning/quick/_scout-faceplate-trend-research.md
@.cursor/rules/perspective-reference.mdc
@.cursor/rules/perspective-css-only.mdc
@.cursor/rules/perspective-ticket-logger.mdc
@.cursor/rules/perspective-json-newlines.mdc
@.cursor/rules/hbt-to-shared.mdc
@.cursor/rules/ignition-resource-signatures.mdc
@.cursor/rules/ignition-8-3-scan-api.mdc

# BH targets (edit these)
@gateways/standard/data/projects/BH/com.inductiveautomation.perspective/views/01_Popups/00_Faceplates/Faceplate/view.json
@gateways/standard/data/projects/BH/com.inductiveautomation.perspective/views/01_Popups/00_Faceplates/_Assets/Trend/view.json
@gateways/standard/data/projects/BH/com.inductiveautomation.perspective/views/01_Popups/00_Faceplates/_Assets/Alarms/view.json
@gateways/standard/data/projects/BH/com.inductiveautomation.perspective/views/01_Popups/00_Faceplates/_Assets/AlarmConfiguration/view.json
@gateways/standard/data/projects/BH/com.inductiveautomation.perspective/stylesheet/stylesheet.css

# Scout reference (copy shape — do not invent Apex/browse schema from memory)
@C:/Program Files/Inductive Automation/Perspective-8-3-Scout/data/projects/ScoutMotors/com.inductiveautomation.perspective/views/01_Popups/00_Faceplates/_Assets/Main/view.json
@C:/Program Files/Inductive Automation/Perspective-8-3-Scout/data/projects/ScoutMotors/com.inductiveautomation.perspective/views/01_Popups/00_Faceplates/Alarm/Alarms/view.json

# Working BH Alarm Status Table (type string reference)
@gateways/standard/data/projects/BH/com.inductiveautomation.perspective/views/00_Pages/Alarms/_Assets/AlarmStatusTable/view.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: CAS + Alarms type + tab contrast + Trend params</name>
  <files>gateways/standard/data/projects/BH/com.inductiveautomation.perspective/views/01_Popups/00_Faceplates/Compressor/Controls/resource.json, gateways/standard/data/projects/BH/com.inductiveautomation.perspective/views/01_Popups/00_Faceplates/Compressor/Configuration/resource.json, gateways/standard/data/projects/BH/com.inductiveautomation.perspective/views/01_Popups/00_Faceplates/_Assets/Alarms/view.json, gateways/standard/data/projects/BH/com.inductiveautomation.perspective/views/01_Popups/00_Faceplates/Faceplate/view.json, gateways/standard/data/projects/BH/com.inductiveautomation.perspective/stylesheet/stylesheet.css</files>
  <read_first>
    - `.planning/quick/_faceplate-diag.md` (CAS root cause; paths already correct)
    - Faceplate `view.json` tab buttons Controls / Configuration / Trend / Alarm Configuration / Alarms (`textStyle.classes`)
    - `_Assets/Alarms/view.json` AlarmStatusTable type + source filter
    - `stylesheet.css` `.psc-font-button` and `.psc-faceplate-button*`
  </read_first>
  <action>
  1. **CAS repair for Controls/Configuration (D-01):** Do not change EmbeddedView path strings — diagnosis confirmed they are correct. Run `python scripts/repair-resource-signatures.py` so `Compressor/Controls` and `Compressor/Configuration` digests land in `projects/.resources/` and `resource.json` signatures match. Confirm with `--check` exit 0 for those two (full check may still fail until later tasks finish). Then POST `/data/api/v1/scan/projects` using repo `.env` / `.ignition-scan.json` (Name:plaintextKey → `X-Ignition-API-Token`). Never invent signatures by hand.

  2. **Alarms component type (D-02):** In `_Assets/Alarms/view.json`, set the Alarm Status Table component `type` to `ia.display.alarmstatustable` (match BH `00_Pages/Alarms/_Assets/AlarmStatusTable` and Scout Faceplate Alarms). Keep the existing source filter expression that builds `prov:…:/tag:…*` from `params.tagPath`. Preserve ticket logger on root if already present.

  3. **Tab text contrast (D-04):** On Faceplate tab buttons named Controls, Configuration, Trend, Alarm Configuration, and Alarms only — remove the inverse button font class from `props.textStyle.classes`. Do **not** strip that class from header chrome (Close / title buttons that sit on dark `container-button`). Prefer CSS-only: add a faceplate tab label class in Advanced Stylesheet (e.g. `.psc-faceplate-tab-text { color: var(--text); }`) and set tab `textStyle.classes` to that class (or leave empty and rely on higher-specificity rules under `.psc-faceplate-button` / `-selected` targeting the label text). Header Close may keep inverse font on dark chrome.

  4. **Shell → Trend params (D-03 wiring):** On Faceplate, add optional input param `hiddenFromTrend` (default empty string). On EmbeddedView, always pass `params.tagPath` (already bound) and add `params.hiddenTags` bound from `view.params.hiddenFromTrend` (property or expr). Keep path expression unchanged. Do not introduce AdhocTrend from the shell.
  </action>
  <verify>
    <automated>python -c "import json,hashlib; from pathlib import Path
root=Path('gateways/standard/data/projects')
cas=root/'.resources'
for rel in ['BH/com.inductiveautomation.perspective/views/01_Popups/00_Faceplates/Compressor/Controls/view.json','BH/com.inductiveautomation.perspective/views/01_Popups/00_Faceplates/Compressor/Configuration/view.json']:
 p=root/rel; h=hashlib.sha256(p.read_bytes()).hexdigest(); assert (cas/h).exists(), (rel,h[:16])
alarms=json.loads(Path('gateways/standard/data/projects/BH/com.inductiveautomation.perspective/views/01_Popups/00_Faceplates/_Assets/Alarms/view.json').read_text(encoding='utf-8'))
def walk(n,acc):
 if isinstance(n,dict):
  if n.get('meta',{}).get('name')=='AlarmStatusTable': acc.append(n.get('type'))
  for c in n.get('children') or []: walk(c,acc)
acc=[]; walk(alarms['root'],acc); assert acc==['ia.display.alarmstatustable'], acc
fp=json.loads(Path('gateways/standard/data/projects/BH/com.inductiveautomation.perspective/views/01_Popups/00_Faceplates/Faceplate/view.json').read_text(encoding='utf-8'))
assert 'hiddenFromTrend' in fp.get('params',{})
tabs={'Controls','Configuration','Trend','Alarm Configuration','Alarms'}
def find_tabs(n,out):
 if isinstance(n,dict):
  name=n.get('meta',{}).get('name')
  if name in tabs:
   ts=(n.get('props') or {}).get('textStyle') or {}
   out[name]=ts.get('classes','')
  for c in n.get('children') or []: find_tabs(c,out)
out={}; find_tabs(fp['root'],out)
for t in tabs:
 assert t in out, t
 assert 'font-button' not in str(out[t]), (t,out[t])
emb=None
def find_emb(n):
 global emb
 if isinstance(n,dict):
  if n.get('meta',{}).get('name')=='EmbeddedView': emb=n
  for c in n.get('children') or []: find_emb(c)
find_emb(fp['root']); assert emb is not None
pc=emb.get('propConfig') or {}
assert 'props.params.hiddenTags' in pc or 'hiddenTags' in json.dumps(emb.get('props',{}).get('params',{}))
css=Path('gateways/standard/data/projects/BH/com.inductiveautomation.perspective/stylesheet/stylesheet.css').read_text(encoding='utf-8')
assert 'faceplate-tab-text' in css or 'faceplate-button' in css
print('task1-ok')" </automated>
  </verify>
  <done>Controls/Config CAS digests present; Alarms uses alarmstatustable; five tab labels lack inverse button font class; Faceplate passes tagPath + hiddenTags; stylesheet supports readable tab text.</done>
</task>

<task type="auto">
  <name>Task 2: Replace Trend stub with Scout ApexChart auto-pens</name>
  <files>gateways/standard/data/projects/BH/com.inductiveautomation.perspective/views/01_Popups/00_Faceplates/_Assets/Trend/view.json, gateways/standard/data/projects/BH/com.inductiveautomation.perspective/views/01_Popups/00_Faceplates/_Assets/Trend/resource.json</files>
  <read_first>
    - Scout `_Assets/Main/view.json` (full propConfig: custom.sensors browse, custom.chartDataAnalog, ApexChart series)
    - BH `_Assets/Trend/view.json` (current AdhocTrend stub — replace)
    - `.planning/quick/_scout-faceplate-trend-research.md`
    - Any BH Kyvis ApexChart view (e.g. AdhocTrend Plot) for module type confirmation only
  </read_first>
  <action>
  Replace BH `_Assets/Trend` content with Scout `_Assets/Main` pattern while **keeping BH path name** `_Assets/Trend` (Faceplate already targets it) — per D-03 and locked CONTEXT (Trend must not use AdhocTrend).

  1. **Params:** `tagPath`, `hiddenTags` (input). No hardcoded FLA/SVP/DisP/Amps list; no Open Adhoc Trend button; no `Alerts.showAdhocTrend` / session AdhocTrend mutation.

  2. **Pen discovery (Scout mechanism — not deviceType map):** `custom.sensors` expr-struct on `tagPath` + `hiddenTags` with script transform that:
     - `system.tag.browse(tagPath, recursive=True)`
     - Skip Folder / UdtType / UdtInstance / Property; skip meta leaf paths with `*.` style
     - Apply `hiddenTags` exclusions (comma/semicolon; full path or provider-stripped)
     - `readBlocking` `.Enabled`, `.HistoryEnabled`, `.DataType`
     - Keep Enabled + HistoryEnabled + non-string/non-date/non-bool
     - Alias = parent folder name if leaf is Value/VALUE, else leaf name
     - Output `analog` list of `{path, alias, aggregate: 'Average'}`

  3. **History + chart:** `custom.chartDataAnalog` queries `system.tag.queryTagHistory` (Wide, Average, returnSize ~100, window from hours dropdown). Root flex column: hours `ia.input.dropdown` (2/4/8/24) + `kyvislabs.display.apexchart` with series transform from history dataset (copy Scout Main series shape). Kyvis is already installed on BH.

  4. **BH conventions:** propConfig keys with scope prefixes; tab-indent every script/`code` line; `\n` only inside JSON strings (no `\r`); rewrite any HBT refs to `shared`; CSS via stylesheet classes only (no Designer path-style Fonts/Label classes from Scout — map to `font-label` / faceplate classes). **Ticket Logger required** on root: `meta.contextMenu`, items/enabled propConfig, `ticketLog` message handler → `shared.Alerts.contextMenuTicketLog` (Scout Main may lack this — add it).

  5. Update `resource.json` files list as needed; do not hand-write signatures — Task 3 runs repair.
  </action>
  <verify>
    <automated>python -c "import json; from pathlib import Path
p=Path('gateways/standard/data/projects/BH/com.inductiveautomation.perspective/views/01_Popups/00_Faceplates/_Assets/Trend/view.json')
t=json.loads(p.read_text(encoding='utf-8'))
s=p.read_text(encoding='utf-8')
assert 'tagPath' in t.get('params',{}) and 'hiddenTags' in t.get('params',{})
assert 'kyvislabs.display.apexchart' in s
assert 'system.tag.browse' in s and 'HistoryEnabled' in s
assert 'queryTagHistory' in s
assert 'showAdhocTrend' not in s and 'AdhocTrend' not in s
assert 'ticketLog' in s and 'shared.Alerts.contextMenuTicketLog' in s
assert 'HBT' not in s
assert '\\r\\n' not in s
# tab-indent check on one script body
found=False
for key,pc in (t.get('propConfig') or {}).items():
 if 'sensors' in key:
  for tr in (((pc.get('binding') or {}).get('transforms')) or []):
   if tr.get('type')=='script':
    code=tr.get('code','')
    for line in code.split(chr(10)):
     if line.strip() and not line.startswith(chr(9)):
      raise SystemExit('non-tab-indented: '+repr(line[:40]))
    found=True
assert found, 'custom.sensors script missing'
print('task2-ok')" </automated>
  </verify>
  <done>Trend is Scout-style in-popup ApexChart with browse auto-pens; no AdhocTrend; ticket logger present; BH script/JSON conventions honored.</done>
</task>

<task type="auto">
  <name>Task 3: History + Alarm Config + signatures + scan</name>
  <files>gateways/standard/data/config/resources/core/ignition/tag-type-definition/default/Devices/udts.json, gateways/standard/data/config/resources/core/ignition/tag-definition/default/Compressors/udts.json, gateways/standard/data/projects/BH/com.inductiveautomation.perspective/views/01_Popups/00_Faceplates/_Assets/AlarmConfiguration/view.json</files>
  <read_first>
    - `Devices/udts.json` Compressor FLA/SVP/DisP/Amps Value members
    - `Compressors/udts.json` instance Value history flags (may already be true — still enforce)
    - `_Assets/AlarmConfiguration/view.json` current stub list
    - `_Root/Analog` history defaults for reference shape
  </read_first>
  <action>
  1. **Compressor analog history (D-06):** On UDT type `Devices/Compressor`, for FLA, SVP, DisP, and Amps `Value` atomic tags, set `historyEnabled: true`, `historyProvider: "historian"`, and match existing deadband/sampleMode style used on Compressors instances / `_Root/Analog` (Absolute deadband ~0.01, OnChange). On every instance under `tag-definition/default/Compressors/udts.json`, ensure those four Value tags likewise have history enabled + historian provider (COMP-01 already true — fix any instance that differs). Do not enable history on Document/SummaryInstances or boolean alarm bits.

  2. **Alarm Configuration (D-05):** Low-effort improvement only — do **not** port full Scout AlarmConfiguration editor. Replace the raw full-path newline dump with a short readable list: count in title (e.g. "Alarm configuration (N)"), and for each alarmed tag show a short alias (last path segment or parent/leaf). Keep a one-line honest stub note that full setpoint editors are deferred. If anything risks breaking the tab, keep the stub but with the clearer title/empty-state copy. Preserve ticket logger.

  3. **Signatures + scans (D-07):** After all project + tag edits: `python scripts/repair-resource-signatures.py` then `python scripts/repair-resource-signatures.py --check` (exit 0). POST scan/projects and scan/config so Perspective views and tag definitions reload. Smoke: Faceplate for COMP-01 — Controls and Configuration resolve; Alarms table mounts; Trend shows ApexChart with pens for history-enabled analogs; tab labels readable.
  </action>
  <verify>
    <automated>python -c "import json; from pathlib import Path
want={'FLA','SVP','DisP','Amps'}
# UDT type
types=json.loads(Path('gateways/standard/data/config/resources/core/ignition/tag-type-definition/default/Devices/udts.json').read_text(encoding='utf-8'))
comp=next(t for t in types if t.get('name')=='Compressor')
seen={}
for t in comp.get('tags',[]):
 if t.get('name') in want:
  for c in t.get('tags',[]):
   if c.get('name')=='Value':
    seen[t['name']]=c
    assert c.get('historyEnabled') is True, t['name']
    assert c.get('historyProvider')=='historian', t['name']
assert set(seen)==want, seen
# instances
inst=json.loads(Path('gateways/standard/data/config/resources/core/ignition/tag-definition/default/Compressors/udts.json').read_text(encoding='utf-8'))
for u in inst:
 for t in u.get('tags',[]):
  if t.get('name') in want:
   for c in t.get('tags',[]):
    if c.get('name')=='Value':
     assert c.get('historyEnabled') is True, (u.get('name'),t.get('name'))
     assert c.get('historyProvider')=='historian', (u.get('name'),t.get('name'))
ac=Path('gateways/standard/data/projects/BH/com.inductiveautomation.perspective/views/01_Popups/00_Faceplates/_Assets/AlarmConfiguration/view.json').read_text(encoding='utf-8')
assert 'ticketLog' in ac
import subprocess,sys
r=subprocess.run([sys.executable,'scripts/repair-resource-signatures.py'],check=False)
assert r.returncode==0
r=subprocess.run([sys.executable,'scripts/repair-resource-signatures.py','--check'],check=False)
assert r.returncode==0
print('task3-ok')" </automated>
  </verify>
  <done>Compressor FLA/SVP/DisP/Amps history enforced on type + instances; Alarm Config usable/honest; signatures --check clean; projects+config scanned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator session → tag browse / historian | Faceplate Trend browse and queryTagHistory run under authenticated Perspective session against gateway tags |
| Disk project/config → gateway CAS load | Signature/CAS integrity gates which views and tags the gateway will deserialize |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-260730-01 | Information Disclosure | Trend browse under tagPath | low | accept | Same operator already sees device faceplate data; browse scoped to passed tagPath + hiddenTags |
| T-260730-02 | Tampering | EmbeddedView path / CAS digests | medium | mitigate | Only repair via `repair-resource-signatures.py`; verify --check exit 0; scan before claiming load |
| T-260730-03 | Denial of Service | Recursive browse + history query | low | mitigate | Reuse Scout filters (Enabled/HistoryEnabled/numeric) and returnSize ~100; hiddenTags prune |
| T-260730-SC | Tampering | npm/pip installs | low | accept | No new package-manager dependencies |
</threat_model>

<verification>
- Task automated asserts all pass.
- `python scripts/repair-resource-signatures.py --check` exit 0.
- Manual smoke: open shared Faceplate on `[default]Compressors/COMP-01` — Controls/Configuration load; Alarms table present; Trend inline chart with pens; tab labels readable; Alarm Config does not error.
</verification>

<success_criteria>
D-01..D-07 satisfied: CAS-backed Controls/Config, alarmstatustable Alarms, Scout-style Trend (no AdhocTrend), readable tabs, honest/improved Alarm Config, compressor analog history, signatures + scan clean with views loading.
</success_criteria>

<output>
Create `.planning/quick/260730-l0s-fix-shared-faceplate-broken-tab-view-pat/260730-l0s-SUMMARY.md` when execution completes.
</output>
