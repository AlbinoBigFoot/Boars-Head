# Handoff: ticket 12705134046 — No TagPath: Navigation

## Monday

| | |
|--|--|
| Item id | `12705134046` |
| Title | No TagPath: Navigation |
| URL | https://oneshotautomation-company.monday.com/boards/18423731526/pulses/12705134046 |
| View path (ticket) | `00_Pages/00_Docked/Navigation` |

## Branch

`ticket/12705134046-no-l5k-ops-nav` (**not on main**)

## Summary of intent / what was done

Operators opening **Groveport → Operations** saw **L5K Parser** in the nav tree. Expected: Operations should not include the L5K parser (engineering tool).

Removed the **L5K Parser** nav item from both Operations item lists inside the docked Navigation view (primary tree + secondary/duplicate tree binding in the same `view.json`). Left the L5K tool view, page route (`/tools/l5k-parser`), and `L5K.Parser` script intact so the tool remains reachable by URL if needed.

## Files changed

- `gateways/standard/data/projects/BH/com.inductiveautomation.perspective/views/00_Pages/00_Docked/Navigation/view.json`
- `gateways/standard/data/projects/BH/com.inductiveautomation.perspective/views/00_Pages/00_Docked/Navigation/resource.json` (signature repair)

Not changed (intentionally):

- `page-config` route `/tools/l5k-parser`
- `98_Configuration/Tools/L5K/*` views
- `TempNav` (still has L5K under Operations if that dock is ever used)

## How to verify

1. Checkout `ticket/12705134046-no-l5k-ops-nav`
2. `python scripts/repair-resource-signatures.py --check --path gateways/standard/data/projects/BH/com.inductiveautomation.perspective/views/00_Pages/00_Docked/Navigation/resource.json`
3. POST scan/projects if needed; hard-refresh BH client (`http://127.0.0.1:19088/data/perspective/client/BH/`)
4. Open **Groveport** nav → expand **Operations**
5. Expect: Alarm Journal, Trending, Saved Trends, Audit Log — **no L5K Parser**

## Follow-up fix (nav expand crash)

After the first L5K nav-item removal, expand/selection sync could throw when `props.selection` was empty (`selection[0]` IndexError in Tree `custom.itemPath` onChange), and residual L5K entries remained in TempNav + `_Config/Navigation` Document tag defaults.

Follow-up on this branch:

- Hardened Navigation + TempNav itemPath onChange (safe empty selection; assign `selection = [path]`).
- Removed L5K from TempNav Operations and `_Config/Navigation` Document defaultValue.
- L5K tool view/route unchanged (`/tools/l5k-parser`).

## Known risks / incomplete

- Direct navigation to `/tools/l5k-parser` still works (by design).
- Live gateway Document **value** (if already written at runtime) may still need a write/flush of `[default]_Config/Navigation` after config scan, or restart, if an old L5K item was persisted in memory.

## Draft PR

`gh` was not authenticated on this machine. Branch is pushed; open a draft PR manually:

https://github.com/AlbinoBigFoot/Boars-Head/pull/new/ticket/12705134046-no-l5k-ops-nav

## Continue from here

1. Open/review draft PR from the link above; merge only after local nav check.
2. Optional follow-up: remove L5K from TempNav or move the tool under a Configuration/admin-only section.
3. Local WIP was stashed before this branch as `stash@{0}: wip-before-ticket-12705134046` (RCP1 tags / `.resources` churn / `.env.example`) — restore with `git stash pop` only after leaving this ticket branch if you still need that work.
