# Handoff: Monday ticket 12654167899

## Monday

| | |
|--|--|
| Item id | `12654167899` |
| Title | Evaporators/EV-01/Status/Value |
| URL | https://oneshotautomation-company.monday.com/boards/18423731526/pulses/12654167899 |
| Reporter | Dylan Jones |
| View called out | `03_Elements/01_Status/StatusIndicator` |

## Branch

`ticket/12654167899-ev01-status` (**not on main**)

Related prior work cherry-picked from `ticket/12652699666-evap-status-modes` (mode codes / enum 6–9 / sim demos).

## Summary of intent / what was done

1. **Evaporator StatusIndicator modes** (status changes apply to evaporators; non-EV keep Run-shaped mapping):
   - `COOL`, `DEF`, `OFF`, `IDLE`, `Operator`, `FLT`
   - Defrost steps: `1.PD` / `2.HG` / `3.BLD` / `3.FD`
2. **Tooltips** on StatusIndicator with parenthetical descriptions (Cooling, Pump Down, Achieved Temp Setpoint, Unit disabled, …).
3. **Defrost stages** render as two lines: primary `DEF`, secondary stage code under it.
4. **AlarmHeader** (`03_Elements/01_Status/AlarmHeader`): former label-row alarm badge now overlays the device Graphic (Scout-style) with priority-colored `alarm-border`.
5. **IconHeader** (`03_Elements/01_Status/IconHeader`): empty shell in the old alarm-icon slot. Symbols for Fault / Operator / Disabled / OOS are a **separate ticket** (`custom.icons.*` stubs ready).
6. Updated all device components (Evaporator / Dual / Triple, Pump, ExhaustFan, CoolingTower, Compressor) to the same LabelHeader + Graphic overlay format.
7. Stylesheet: `alarm-border`, `alarm-priority-*`, `alarm-header`, `icon-header`.
8. Tag/sim/docs updates from the prior mode-mapping ticket retained.

## Files changed (primary)

- `…/StatusIndicator/view.json` (+ resource)
- `…/AlarmHeader/` (new)
- `…/IconHeader/` (new)
- `…/01_Devices/{Evaporator,EvaporatorDual,EvaporatorTriple,Pump,ExhaustFan,CoolingTower,Compressor}/view.json`
- `…/stylesheet/stylesheet.css`
- `docs/evaporator-hmi-components.md`
- Evaporator UDT / Devices UDT / `sim/build_plant_sim.py` / `sim/bh-plant-sim.csv` (from cherry-pick)

## How to verify

1. Checkout `ticket/12654167899-ev01-status` and open Perspective client: `http://127.0.0.1:19088/data/perspective/client/BH/`
2. Evaporators Overview:
   - EV-02 → `COOL` tooltip “Cooling”
   - EV-04 → `DEF`
   - EV-09 → `DEF` with `1.PD` under + tooltip “Pump Down”
   - EV-12 → `Operator`
   - EV-14 / 15 / 10 → stage lines for HG / FD / BLD
3. Hover StatusIndicator → tooltip text matches ticket parentheses.
4. Force `_Alarms/_Active` on an EV → AlarmHeader overlays graphic corner; colored border appears; label-row IconHeader stays empty (no alarm icon there).
5. Non-EV devices (Pump/CT/etc.) still show Run/Idle/Fault/OFF mapping; AlarmHeader/IconHeader layout matches evaporators.

## Known risks / incomplete

- **IconHeader symbols** not designed yet (explicit follow-up ticket). Slot is structural only.
- Defrost “stage under DEF” uses Status enum 6–9; if PLC packs stage differently, mapping may need another pass.
- Operator detection for IconHeader is not wired (status text already shows Operator); waiting on dedicated Manual/OOS tags + icons.
- Prior ticket `12652699666` is a subset; consider closing/linking it once this branch is reviewed.

## Continue from here

1. Review draft PR visually in client (all four themes).
2. When Figma/icon ticket lands: fill `IconHeader` slots (Fault / Operator / Disabled / OOS) and optionally drop temporary material placeholders.
3. Optionally reconcile Monday `12652699666` with this item (same StatusIndicator theme).
4. Merge only after Dylan local review — do **not** auto-merge to `main`.
